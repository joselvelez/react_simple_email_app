import React from 'react';
import UserMenu from './Usermenu';

const Header = () => (
    <header className="Header">
        <h2>MyMail</h2>
        <UserMenu />
    </header>
);

export default Header;